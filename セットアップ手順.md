# エビデンスゼミ ― 本番につなぐ手順

**ターミナルは使いません。全部ブラウザの操作です。** 20分ほどで終わります。

つなぐと、こうなります。

- 会員が自分でアカウントを作ってログインできる（本物のログイン）
- どの端末から見ても同じデータ。パソコンが変わっても続きから使える
- 課題の解錠・配信の掲載期限・会員資格の遮断が、実データの上で動く

**お金のやりとり（Stripe）はまだつなぎません。** モニターは入会金0円＋初月無料なので、
決済なしで走らせられます。決済は契約後、知上会さま名義の Stripe 口座ができてから足します。

---

## 0. 用意するもの

- Google アカウント（Firebase のログインに使います）
- 20分

料金はかかりません。**Blaze プラン（カード登録）は不要**です。
Cloud Functions を使わない作りにしてあるので、無料の Spark プランのままで動きます。

---

## 1. Firebase のプロジェクトを作る

1. <https://console.firebase.google.com> を開いて Google アカウントでログイン
2. **「プロジェクトを作成」**
3. 名前は `ezemi` など。**Google アナリティクスはオフ**でよい
4. 「続行」を押していくと出来上がります

---

## 2. データベース（Firestore）を作る

1. 左メニュー **「構築 → Firestore Database」** → **「データベースの作成」**
2. ロケーション … **asia-northeast1（東京）**
3. モード … **本番環境モードで開始**
4. 出来たら上の **「ルール」** タブを開く
5. 中身を**全部消して**、このフォルダの **`firestore.rules` の中身を丸ごと貼り付け**、**「公開」**

> ここが一番大事です。「誰が何を読めるか」はこのルールで決まります。
> 画面のコードは誰でも読めるので、守りはここに集約してあります。
> 貼り忘れると、**会員の提出物が他人から読める状態**になります。

---

## 3. ログイン（Authentication）を有効にする

1. 左メニュー **「構築 → Authentication」** → **「始める」**
2. **「メール／パスワード」** を選んで **有効にする** → 保存
3. 上の **「設定」** タブ → **「承認済みドメイン」**
4. **`playmark0227-svg.github.io` を追加**

> 4番を忘れると「このドメインは許可されていません」と出てログインできません。
> あとで独自ドメイン（知上会さまのHP）に載せるときも、同じ場所にそのドメインを足します。

---

## 4. 設定をコピーする

1. 左上の **⚙（歯車）→ プロジェクトの設定**
2. 下のほうの **「マイアプリ」** で **`</>`（ウェブ）** を選ぶ
3. アプリのニックネームは `ezemi-web` など。**Hosting のチェックは不要**
4. 出てくる `const firebaseConfig = { ... };` の **`{` から `}` までをコピー**

---

## 5. 管理画面に貼り付ける

1. <https://playmark0227-svg.github.io/ezemi-preview/admin.html> を開く
2. パスワード `ezemi` で入る
3. 左メニュー **「データ」** → **「本番のデータ置き場（Firebase）」**
4. さっきコピーした設定を貼り付けて **「つなぐ」**
5. 画面が読み込み直され、ログイン画面に変わります

---

## 6. 自分のアカウントを作る

1. <https://playmark0227-svg.github.io/ezemi-preview/#/join> を開く
2. ご自分の名前・メール・パスワードを入れ、クーポンは **`MONITOR`**
3. 「登録して会員ページへ」

これで Authentication にアカウントができました。次でこのアカウントを管理者にします。

---

## 7. 自分を管理者にする

1. Firebase コンソール → **Authentication → Users**
2. さっき作ったご自分の行の **「ユーザーUID」をコピー**（`abc123...` のような文字列）
3. **Firestore Database → データ** タブ
4. **「コレクションを開始」** → コレクションID … **`admins`**
5. **ドキュメントID … いまコピーしたUIDを貼り付け**
6. フィールドは空でよい（`id` に同じUIDを入れておくと後で分かりやすい）→ 保存

> `admins` に入っている人だけが管理画面を開けます。
> このコレクションはルールで**誰にも書き込めない**ようにしてあるので、
> 管理者を増やすときは必ずこのコンソールから手で足してください。

---

## 8. 講座と文言を書き込む

1. 管理画面を開き直して、いまのメールとパスワードでログイン
2. 左メニュー **「データ」** → **「講座・教材・文言を書き込む」**

講座7回・教材4点・公開レポート3本・規約の文言・料金設定が Firestore に入ります。
**最初の1回だけ**押してください。

---

## 9. 動くか確かめる

| 確かめること | やりかた |
|---|---|
| 会員が入れる | 別のブラウザ（またはシークレットウィンドウ）で `#/join` から入会 |
| 課題で次回が開く | 会員ページ → 動画講座 → 第1回の課題を出す → 第2回が開く |
| 事務局に届く | 管理画面のホームに「課題の提出」が出る |
| 別の端末で同じ | スマホで同じメールとパスワードでログイン |
| 他人のものは見えない | 会員のまま管理画面を開こうとすると弾かれる |

---

# よくあるつまずき

**「このドメインは許可されていません」** … 手順3-4の承認済みドメインが抜けています。

**管理画面に入れない** … `admins` のドキュメントIDが、Authentication の UID と一字一句同じか確かめてください。
メールアドレスではなく **UID** です。

**講座が空っぽ** … 手順8を押していません。

**「保存できませんでした：permission-denied」** … ルール（手順2-5）が貼られていないか、古いままです。
`firestore.rules` を貼り直して「公開」を押してください。

**元に戻したい** … 管理画面 → データ → 「接続を解除する」。
この端末のブラウザから設定を消すだけで、Firebase 側のデータは残ります。

---

# このあと（決済をつなぐとき）

契約が決まって、知上会さま名義の Stripe 口座ができたら足します。

1. Stripe で「入会金（1回きり）」と「月額（サブスクリプション）」の商品を作る
2. 毎月の課金・カードが通らなかったときの再試行・督促メールは **Stripe 側の機能で持つ**
   （そのため、こちらにサーバの定期処理は要りません）
3. 会員資格の同期だけ、Stripe の Webhook から Firestore の `members/{uid}.billing` を書く
   → ここだけ Cloud Functions（Blaze プラン）か、外部の小さな受け口が必要になります
4. 入会画面を Stripe Checkout に差し替える（`assets/js/site.js` の `checkout()` の位置）

会員資格の判定は「いま計算する」作りにしてあるので、`billing.state` と `periodEnd` を
Stripe の状態に合わせて書き込むだけで、閲覧の遮断は自動で正しくなります。

---

# 引き継ぎについて（発注仕様書 0-3）

Firebase プロジェクトは **知上会さま名義の Google アカウントで作る**のが本来の形です。
先に久保のアカウントで作った場合は、Firebase コンソールの
**プロジェクトの設定 → ユーザーと権限** から知上会さまを「オーナー」に追加し、
あとで久保を外せば、そのまま引き継げます（データは動かさずに済みます）。
